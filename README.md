# Bayesian-statistics
A simple Bayesian statistics example estimating the fraction of a population that supports more gun control. Uses a Beta prior, updates with survey data, and computes the posterior distribution along with the credible interval.
